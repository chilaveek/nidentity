{
  "name": "nidshop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix client",
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "server": "dotnet watch run --project server-dotnet",
    "start": "npm run dev"
  }
}
